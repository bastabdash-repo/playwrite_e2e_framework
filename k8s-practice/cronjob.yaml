apiVersion: batch/v1
kind: CronJob
metadata:
  name: playwright-cronjob
spec:
  schedule: "*/5 * * * *" # every 5 minutes
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: playwright
              image: bastabdash/playwright-e2e:latest
          restartPolicy: Never
