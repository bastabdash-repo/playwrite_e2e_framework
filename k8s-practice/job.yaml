apiVersion: batch/v1
kind: Job
metadata:
  name: playwright-job
spec:
  template:
    spec:
      containers:
      - name: playwright
        image: bastabdash/playwright-e2e:latest
      restartPolicy: Never